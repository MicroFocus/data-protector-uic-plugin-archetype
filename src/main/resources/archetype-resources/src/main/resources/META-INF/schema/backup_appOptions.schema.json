{
	"$schema": "http://json-schema.org/draft-04/schema#",
	"title": "Backup appOptions",
	"description": "appOptions in backup specification and backup request; For JSON Schema, see https://json-schema.org/specification.html",
	"type": "object",
	"properties": {
		"appName": {
			"description": "Name of the data to protect",
			"type": "string"
		},
		"appId": {
			"description": "ID of the data to protect",
			"type": "string"
		}
	},
	"required": [
		"appName",
		"appId"
	]
}
